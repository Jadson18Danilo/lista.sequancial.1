//Faça um algoritmo que peça dois números e imprima a soma.
const prompt = require("prompt-sync")()

const numero1 = prompt(`Digite o primeiro valor:`)

const numero2 = prompt(`Digite o primeiro valor:`)

const soma = parseInt(prompt(numero1 + numero2))

console.log(`A soma do primeiro valor mais o segundo é ${soma}`)